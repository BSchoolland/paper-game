{
  "name": "paper-game",
  "private": true,
  "type": "module",
  "workspaces": ["shared", "client", "server"],
  "scripts": {
    "predev": "fuser -k 3001/tcp 5173/tcp 2>/dev/null; true",
    "dev": "bun --filter '*' dev",
    "typecheck": "bunx tsc",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "typescript-eslint": "^8.59.2"
  }
}
