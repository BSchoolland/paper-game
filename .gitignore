.env
.env.*
node_modules
dist
bun.lock
.tsbuildinfo
.vite
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite.bak-*
ScriptableByteArray*
scripts/__pycache__/
# Local scratch: agent worktrees (embedded git repos) and UI prototypes — not part of the app.
.claude/worktrees/
ingame-experiment/
lobby-experiment/
# Combat replays — large, regenerable via sim-battle / hero-arena, viewed only by the legacy client.
client-legacy/public/replay*.json
client-legacy/public/tmp/
dimension-generator/diffusion-bundles/**/output.png

# Balance test artifacts (generated by hero-arena/src/t2/balance-test.ts and item-test.ts)
balance-report-dim-*.json
balance-logs-dim-*/
item-report-dim-*.json
item-logs-dim-*/
dim-*-spec.jsonl
dim-*-enemy-agent.jsonl
dim-*-item-agent.jsonl

# Encounter map art is served from the CDN (CloudFront/S3), not git — too large
# to version. Collision masks and manifests stay tracked (server reads masks).
public/sprites/maps/**/*.png
!public/sprites/maps/**/*.mask.png
