/**
 * Forwarding shim — the canonical toolkit lives in `shared/src/ai/sovereign-helpers.ts` so the
 * main game can use it. Hero-arena imports continue to work unchanged via this re-export.
 */
export * from "../../shared/src/ai/sovereign-helpers.js";
